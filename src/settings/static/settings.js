const byId=id=>document.getElementById(id);function message(text,error=false){const el=byId("message");el.textContent=text;el.className=`message${error?" error":""}`}async function loadSettings(){try{const response=await fetch("/api/settings");if(response.status===401){location.replace("/login");return}const data=await response.json();if(!response.ok)throw new Error(data.detail||"설정을 불러오지 못했습니다.");byId("openai-state").textContent=data.openai_configured?"등록됨":"미등록";byId("storage-type-value").textContent=data.storage_type==="r2"?"Cloudflare R2":"S3 호환 저장소";byId("endpoint-value").textContent=data.s3_endpoint_url||"미등록";byId("bucket-value").textContent=data.s3_bucket_name||"미등록";byId("access-key-state").textContent=data.s3_access_key_configured?"등록됨":"미등록";byId("secret-key-state").textContent=data.s3_secret_key_configured?"등록됨":"미등록";byId("updated-at").textContent=data.updated_at?new Date(data.updated_at).toLocaleString("ko-KR"):"—";message("")}catch(error){message(error.message,true)}}byId("logout-button").addEventListener("click",async()=>{const response=await fetch("/logout",{method:"POST"}),result=await response.json();location.assign(result.logout_url||"/logged-out")});loadSettings();
