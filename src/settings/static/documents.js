const byId=id=>document.getElementById(id);let documents=[];
function escapeHtml(value){return value.replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]))}
function inline(text){return text.replace(/`([^`]+)`/g,"<code>$1</code>").replace(/!\[([^\]]*)\]\((https?:\/\/[^\s)]+)\)/g,'<img alt="$1" src="$2" loading="lazy">').replace(/\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)/g,'<a href="$2" target="_blank" rel="noopener noreferrer">$1</a>').replace(/\*\*([^*]+)\*\*/g,"<strong>$1</strong>").replace(/~~([^~]+)~~/g,"<del>$1</del>").replace(/(^|\s)\*([^*]+)\*/g,"$1<em>$2</em>")}
function renderMarkdown(source){const escaped=escapeHtml(source.replace(/^---\s*\n[\s\S]*?\n---\s*\n?/,""));const lines=escaped.split("\n"),out=[];let code=false,list=null,quote=false;const close=()=>{if(list){out.push(`</${list}>`);list=null}if(quote){out.push("</blockquote>");quote=false}};for(const line of lines){if(line.startsWith("```")){close();out.push(code?"</code></pre>":"<pre><code>");code=!code;continue}if(code){out.push(line+"\n");continue}const heading=line.match(/^(#{1,6})\s+(.+)$/);if(heading){close();const n=heading[1].length;out.push(`<h${n}>${inline(heading[2])}</h${n}>`);continue}const ul=line.match(/^\s*[-*+]\s+(.+)$/),ol=line.match(/^\s*\d+\.\s+(.+)$/);if(ul||ol){const kind=ul?"ul":"ol";if(list!==kind){close();list=kind;out.push(`<${kind}>`)}out.push(`<li>${inline((ul||ol)[1])}</li>`);continue}if(line.startsWith("&gt; ")){if(!quote){close();quote=true;out.push("<blockquote>")}out.push(`<p>${inline(line.slice(5))}</p>`);continue}if(/^(-{3,}|\*{3,})$/.test(line.trim())){close();out.push("<hr>");continue}if(!line.trim()){close();continue}close();out.push(`<p>${inline(line)}</p>`)}close();if(code)out.push("</code></pre>");return out.join("\n")}
function message(text,error=false){const el=byId("message");el.textContent=text;el.className=`message${error?" error":""}`}
function displayName(doc){return doc.name.replace(/\.md$/i,"")}
function renderList(){const query=byId("document-search").value.trim().toLowerCase(),list=byId("document-list");list.textContent="";const filtered=documents.filter(doc=>doc.path.toLowerCase().includes(query));byId("document-count").textContent=`${filtered.length}개 문서`;for(const doc of filtered){const button=document.createElement("button");button.type="button";button.className="document-item";button.dataset.path=doc.path;const title=document.createElement("strong");title.textContent=displayName(doc);const path=document.createElement("span");path.textContent=doc.path;button.append(title,path);button.addEventListener("click",()=>openDocument(doc.path,button));list.append(button)}if(!filtered.length){const empty=document.createElement("p");empty.className="list-empty";empty.textContent=query?"일치하는 문서가 없습니다.":"저장된 Markdown 문서가 없습니다.";list.append(empty)}}
async function openDocument(path,button){message("문서를 불러오는 중입니다.");try{const response=await fetch(`/api/documents/${path.split("/").map(encodeURIComponent).join("/")}`);if(response.status===401){location.replace("/login");return}const data=await response.json();if(!response.ok)throw new Error(data.detail||"문서를 불러오지 못했습니다.");document.querySelectorAll(".document-item.active").forEach(el=>el.classList.remove("active"));button.classList.add("active");byId("document-meta").textContent=data.path;byId("document-content").innerHTML=renderMarkdown(data.content);message("");history.replaceState(null,"",`/documents?path=${encodeURIComponent(data.path)}`)}catch(error){message(error.message,true)}}
async function loadDocuments(){try{const response=await fetch("/api/documents");if(response.status===401){location.replace("/login");return}const data=await response.json();if(!response.ok)throw new Error(data.detail||"문서 목록을 불러오지 못했습니다.");documents=data.documents;renderList();message("");const requested=new URLSearchParams(location.search).get("path");const target=documents.find(doc=>doc.path===requested);if(target){const button=[...document.querySelectorAll(".document-item")].find(el=>el.dataset.path===target.path);if(button)openDocument(target.path,button)}}catch(error){message(error.message,true)}}
byId("document-search").addEventListener("input",renderList);byId("logout-button").addEventListener("click",async()=>{await fetch("/logout",{method:"POST"});location.replace("/login")});loadDocuments();
