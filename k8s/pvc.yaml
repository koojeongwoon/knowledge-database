apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: llm-wiki
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # OrbStack의 기본 스토리지 클래스(default storage class)를 사용하여 로컬 디스크에 영속 저장합니다.
