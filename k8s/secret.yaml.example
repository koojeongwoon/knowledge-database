apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: llm-wiki
type: Opaque
stringData:
  # 사용할 실제 데이터베이스 비밀번호를 여기에 적어줍니다. (비밀번호 설정 후 파일명을 secret.yaml로 변경하여 사용)
  POSTGRES_PASSWORD: "your_secure_password_here"
